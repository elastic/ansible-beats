---
# handlers file for beats

- name: restart {{ beat }}
  service:
    name: '{{ beat }}'
    state: restarted
  when:
    - beats_start_service | bool
    - beats_restart_on_change | bool
    - not beats_started.changed | bool
